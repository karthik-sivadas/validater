---
phase: 10-quality-and-coverage
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/core/src/ai/__tests__/cost-tracker.test.ts
  - packages/core/src/ai/__tests__/rate-limiter.test.ts
  - packages/core/src/ai/__tests__/client.test.ts
  - packages/core/src/schemas/__tests__/test-step.test.ts
  - packages/core/src/schemas/__tests__/locator.test.ts
  - packages/core/src/schemas/__tests__/test-suite.test.ts
  - packages/core/src/dom/__tests__/simplifier.test.ts
  - packages/core/src/dom/__tests__/extractor.test.ts
  - packages/core/src/execution/__tests__/assertions.test.ts
  - packages/core/src/execution/__tests__/viewport-presets.test.ts
  - packages/core/src/execution/__tests__/step-runner.test.ts
  - packages/core/src/execution/__tests__/step-executor.test.ts
  - packages/core/src/locators/__tests__/mapper.test.ts
  - packages/core/src/locators/__tests__/validator.test.ts
  - packages/core/src/locators/__tests__/healer.test.ts
  - packages/worker/src/browser/__tests__/memory-monitor.test.ts
  - packages/worker/src/activities/__tests__/persist-results.test.ts
  - packages/worker/src/activities/__tests__/generate-steps.test.ts
  - packages/worker/src/video/__tests__/storage.test.ts
  - packages/worker/src/reports/__tests__/html-generator.test.ts
autonomous: true

must_haves:
  truths:
    - "Core business logic modules (ai, schemas, dom, execution, locators) achieve 90%+ line coverage with passing tests"
    - "Core modules test both happy path and error/edge cases for every exported function"
    - "Worker service modules (activities, browser, reports, video) achieve 75%+ line coverage with passing tests"
    - "All 20 test files pass when running pnpm test from root with 0 failures"
    - "Missing modules (step-executor, healer, client, generate-steps, video/storage) have dedicated test files"
  artifacts:
    - path: "packages/core/src/ai/__tests__/cost-tracker.test.ts"
      provides: "Tests for calculateCost and CostTracker"
      min_lines: 50
    - path: "packages/core/src/ai/__tests__/rate-limiter.test.ts"
      provides: "Tests for createApiQueue and queuedRequest"
      min_lines: 30
    - path: "packages/core/src/ai/__tests__/client.test.ts"
      provides: "Tests for createAIClient provider selection"
      min_lines: 30
    - path: "packages/core/src/schemas/__tests__/test-step.test.ts"
      provides: "Zod schema validation tests"
      min_lines: 40
    - path: "packages/core/src/dom/__tests__/simplifier.test.ts"
      provides: "DOM simplification tests with HTML input/output"
      min_lines: 60
    - path: "packages/core/src/execution/__tests__/assertions.test.ts"
      provides: "Assertion type tests with mock Page/Locator"
      min_lines: 80
    - path: "packages/core/src/execution/__tests__/step-executor.test.ts"
      provides: "Tests for executeSteps orchestration and onStepComplete callback"
      min_lines: 40
    - path: "packages/core/src/locators/__tests__/mapper.test.ts"
      provides: "Locator mapping tests for all 7 locator types"
      min_lines: 60
    - path: "packages/core/src/locators/__tests__/healer.test.ts"
      provides: "Tests for healLocator and healStepLocators"
      min_lines: 50
    - path: "packages/worker/src/browser/__tests__/memory-monitor.test.ts"
      provides: "Memory health check tests"
      min_lines: 30
    - path: "packages/worker/src/activities/__tests__/persist-results.test.ts"
      provides: "Factory DI activity tests"
      min_lines: 30
    - path: "packages/worker/src/activities/__tests__/generate-steps.test.ts"
      provides: "Tests for generateSteps activity with mocked core"
      min_lines: 30
    - path: "packages/worker/src/video/__tests__/storage.test.ts"
      provides: "Tests for saveVideo and getVideoPath"
      min_lines: 25
  key_links:
    - from: "packages/core/src/**/__tests__/*.test.ts"
      to: "packages/core/src/__test-utils__/mock-page.ts"
      via: "import createMockPage/createMockLocator"
      pattern: "createMock(Page|Locator)"
    - from: "packages/worker/src/**/__tests__/*.test.ts"
      to: "packages/core/src/__test-utils__/mock-db.ts"
      via: "import createMockDb"
      pattern: "createMockDb"
---

<objective>
Write unit tests for business logic (core package) and service layer (worker package) modules.

Purpose: Achieve the 90%+ business logic and 75%+ service layer coverage targets. Covers all core exported functions and worker service modules including previously missing step-executor, healer, client, generate-steps, and video/storage.
Output: Comprehensive test suites for core (ai, schemas, dom, execution, locators) and worker (browser, activities, reports, video) modules -- 20 test files total.
</objective>

<execution_context>
@/Users/karthiksivadas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karthiksivadas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-and-coverage/10-RESEARCH.md
@.planning/phases/10-quality-and-coverage/10-01-SUMMARY.md

Source files to test:
@packages/core/src/ai/cost-tracker.ts
@packages/core/src/ai/rate-limiter.ts
@packages/core/src/ai/client.ts
@packages/core/src/schemas/test-step.ts
@packages/core/src/schemas/locator.ts
@packages/core/src/schemas/test-suite.ts
@packages/core/src/dom/simplifier.ts
@packages/core/src/dom/extractor.ts
@packages/core/src/execution/assertions.ts
@packages/core/src/execution/viewport-presets.ts
@packages/core/src/execution/step-runner.ts
@packages/core/src/execution/step-executor.ts
@packages/core/src/locators/mapper.ts
@packages/core/src/locators/validator.ts
@packages/core/src/locators/healer.ts
@packages/worker/src/browser/memory-monitor.ts
@packages/worker/src/activities/persist-results.activity.ts
@packages/worker/src/activities/generate-steps.activity.ts
@packages/worker/src/video/storage.ts
@packages/worker/src/reports/html-generator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core AI and schema tests (5 files)</name>
  <files>
    packages/core/src/ai/__tests__/cost-tracker.test.ts
    packages/core/src/ai/__tests__/rate-limiter.test.ts
    packages/core/src/ai/__tests__/client.test.ts
    packages/core/src/schemas/__tests__/test-step.test.ts
    packages/core/src/schemas/__tests__/locator.test.ts
    packages/core/src/schemas/__tests__/test-suite.test.ts
  </files>
  <action>
Create `__tests__/` directories and test files for AI and schema modules. Read each source file first to understand exact exports and signatures.

**ai/cost-tracker.test.ts** (pure functions, no mocks):
- `calculateCost`: known model pricing (claude-sonnet-4-5, claude-haiku-3-5), unknown model fallback to default, zero tokens, cache token pricing (cacheCreationInputTokens, cacheReadInputTokens)
- `CostTracker`: track multiple entries -> getUsageSummary aggregates correctly, getTotalCost returns sum, reset clears entries, empty tracker returns zeroes

**ai/rate-limiter.test.ts** (test queue behavior):
- `createApiQueue`: creates PQueue with default options, custom options respected
- `queuedRequest`: wraps async function through queue, propagates errors, returns result
- `defaultApiQueue`: is a PQueue instance

**ai/client.test.ts** (mock env vars and providers):
- Mock `@ai-sdk/anthropic` and `@openrouter/ai-sdk-provider` with `vi.mock()`
- `createAIClient()` with no OPENROUTER_API_KEY -> uses Anthropic provider with default model claude-sonnet-4-5
- `createAIClient()` with OPENROUTER_API_KEY set -> uses OpenRouter provider with default model moonshotai/kimi-k2.5
- `createAIClient({ model: 'custom-model' })` -> uses custom model name
- Test AI_MODEL env var override
- Use `vi.stubEnv()` to control env vars, restore in afterEach

**schemas/test-step.test.ts** (Zod validation):
- `TestStepSchema`: valid step parses, rejects missing fields, rejects < 2 locators (min(2) constraint), validates action enum values, optional value and assertion fields
- `TestGenerationSchema`: valid generation parses, requires steps array and reasoning
- `TestStepAssertionSchema`: valid assertion types, rejects invalid type

**schemas/locator.test.ts** (Zod validation):
- `LocatorStrategySchema`: valid locator parses, validates type enum (role, text, label, placeholder, testId, css, xpath), requires confidence 0-1, requires reasoning string

**schemas/test-suite.test.ts** (Zod validation):
- Read the schema file first. Test valid suite structures parse, invalid ones reject. Test min/max constraints if present.

IMPORTANT: Use `import { vi, describe, it, expect } from 'vitest'` in all test files. Use `.js` extension in imports for NodeNext compatibility (e.g., `import { calculateCost } from '../cost-tracker.js'`).
  </action>
  <verify>
Run `pnpm --filter @validater/core exec vitest run src/ai/__tests__ src/schemas/__tests__` -- all tests pass.
Check test count: should be 25+ individual test cases across these 6 files.
  </verify>
  <done>6 test files exist for ai/ and schemas/ modules with comprehensive tests covering happy path, error, and edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Core DOM, execution, and locator tests (9 files)</name>
  <files>
    packages/core/src/dom/__tests__/simplifier.test.ts
    packages/core/src/dom/__tests__/extractor.test.ts
    packages/core/src/execution/__tests__/assertions.test.ts
    packages/core/src/execution/__tests__/viewport-presets.test.ts
    packages/core/src/execution/__tests__/step-runner.test.ts
    packages/core/src/execution/__tests__/step-executor.test.ts
    packages/core/src/locators/__tests__/mapper.test.ts
    packages/core/src/locators/__tests__/validator.test.ts
    packages/core/src/locators/__tests__/healer.test.ts
  </files>
  <action>
Create `__tests__/` directories and test files for DOM, execution, and locator modules. Read each source file first.

**dom/simplifier.test.ts** (pure Cheerio, feed HTML strings):
- `simplifyDom`: strips script/style/noscript tags, strips hidden elements, strips SVG/iframe, preserves semantic content (headings, buttons, links, forms), enforces token budget with maxTokenEstimate option, returns html/elements/interactiveElements/pageContext/tokenEstimate, handles empty HTML, handles malformed HTML gracefully

**dom/extractor.test.ts** (pure Cheerio):
- Read source first. Test `extractInteractiveElements` (finds buttons, inputs, links, selects, textareas), `extractSemanticElements` (finds headings, landmarks, forms), `isUtilityClass` (filters Tailwind-like classes: flex, p-4, text-sm vs real classes: login-button, main-nav)

**execution/assertions.test.ts** (mock Page + Locator):
- Import `createMockPage`, `createMockLocator` from `@validater/core/__test-utils__`
- `checkAssertion`: test each assertion type:
  - `visible`: locator.isVisible() returns true -> pass, false -> throws AssertionError
  - `hidden`: locator.isHidden() returns true -> pass, false -> throws
  - `text`: locator.textContent() matches expected -> pass, mismatch -> throws
  - `value`: locator.inputValue() matches expected -> pass, mismatch -> throws
  - `url`: page.url() matches expected -> pass, mismatch -> throws
  - `count`: locator.count() matches expected number -> pass
  - `attribute`: test attribute assertion if implemented
- `AssertionError`: has assertionType, expected, actual properties

**execution/viewport-presets.test.ts** (pure data):
- `VIEWPORT_PRESETS`: desktop has 1920x1080, tablet has 768x1024, mobile has 375x812
- `DEFAULT_VIEWPORTS`: contains 'desktop', 'tablet', 'mobile'
- Each preset has name, width, height, deviceScaleFactor, isMobile, hasTouch

**execution/step-runner.test.ts** (mock Page):
- Read source first. Test `executeStep` dispatches correct Page method per action type:
  - `click` -> locator.click()
  - `fill` -> locator.fill(value)
  - `navigate` -> page.goto(value)
  - `hover` -> locator.hover()
  - `wait` -> page.waitForTimeout()
  - Test assertion checking after action
- Test `resolveLocator` if exported -- tries primary locator first, falls back to alternatives

**execution/step-executor.test.ts** (mock executeStep):
- Mock `./step-runner.js` with `vi.mock()` -- mock `executeStep` to return a StepResult
- `executeSteps(page, steps)`: calls executeStep for each step in order, returns array of StepResult
- Test that all steps execute even when one fails (continues past failure)
- Test default config merging (stepTimeoutMs: 10_000, navigationTimeoutMs: 30_000)
- Test custom config override
- Test `onStepComplete` callback: is called after each step with the result
- Test `onStepComplete` error swallowing: callback that throws does NOT break execution
- Test empty steps array returns empty results

**locators/mapper.test.ts** (mock Page):
- `mapLocatorToPlaywright`: test all 7 locator types:
  - role: "button" -> page.getByRole('button'), "button: Submit" -> page.getByRole('button', { name: 'Submit' })
  - text: -> page.getByText()
  - label: -> page.getByLabel()
  - placeholder: -> page.getByPlaceholder()
  - testId: -> page.getByTestId()
  - css: -> page.locator()
  - xpath: -> page.locator('xpath=...')

**locators/validator.test.ts** (mock Page + Locator):
- Read source first. Test `verifyLocator` with found (count > 0) and not-found (count = 0) scenarios, invalid selector (count throws) returns found: false. Test `verifyStepLocators` validates all locators for a step and returns structured results.

**locators/healer.test.ts** (mock Page, vi.mock dependencies):
- Mock `../ai/client.js`, `../ai/prompts/templates.js`, `./validator.js` with `vi.mock()`
- Import `createMockPage` from `@validater/core/__test-utils__`
- `healLocator(page, step, validationResult)`:
  - Strategy 1: When working alternatives exist (found + isUnique), returns highest-confidence one (no AI call)
  - Strategy 1b: When alternatives found but not unique, returns highest-confidence found locator
  - Strategy 2: When all locators failed, calls AI and verifies suggestions against page
  - Returns null when AI suggestions also fail
  - Returns null when AI call throws (error swallowing)
- `healStepLocators(page, steps, validationResults)`:
  - Passes through steps with valid primary locators unchanged
  - Passes through steps with no validation result unchanged
  - Healed locator that already exists in array -> updates primaryLocatorIndex
  - Healed locator that is new -> appends to locators array and sets primaryLocatorIndex
  - Unhealed step -> keeps original step unchanged
  - Returns new array (does not mutate original)

IMPORTANT: Use `import { vi, describe, it, expect } from 'vitest'` in all test files. Use `.js` extension in imports for NodeNext compatibility.
  </action>
  <verify>
Run `pnpm --filter @validater/core exec vitest run src/dom/__tests__ src/execution/__tests__ src/locators/__tests__` -- all tests pass.
Check test count: should be 40+ individual test cases across these 9 files.
  </verify>
  <done>9 test files exist for dom/, execution/, and locators/ modules. All tests pass. step-executor, healer tests cover all branches.</done>
</task>

<task type="auto">
  <name>Task 3: Worker package service layer tests (5 files)</name>
  <files>
    packages/worker/src/browser/__tests__/memory-monitor.test.ts
    packages/worker/src/activities/__tests__/persist-results.test.ts
    packages/worker/src/activities/__tests__/generate-steps.test.ts
    packages/worker/src/video/__tests__/storage.test.ts
    packages/worker/src/reports/__tests__/html-generator.test.ts
  </files>
  <action>
Create `__tests__/` directories and test files for worker service modules.

**browser/memory-monitor.test.ts**:
- Mock `process.memoryUsage` with `vi.spyOn(process, 'memoryUsage')`
- `checkMemoryHealth`: returns healthy=true when RSS and heap are within defaults, returns healthy=false when RSS exceeds maxRssBytes, returns healthy=false when heap percentage exceeds maxHeapPercentage, respects custom config thresholds, returns correct rssBytes/heapUsedBytes/heapTotalBytes/heapPercentage values

**activities/persist-results.test.ts**:
- Import `createMockDb` from `@validater/core/__test-utils__`
- `createPersistActivities(db)`: returns object with `persistResults` and `updateTestRunStatus` functions
- Read the source file to understand the exact function signatures and behavior
- Test that persistResults calls db.insert with correct table and data shape
- Test that updateTestRunStatus calls db.update with correct status value
- Use vi.fn() mocks on the db chain methods and verify they were called

**activities/generate-steps.test.ts**:
- Mock `@validater/core` with `vi.mock()` -- mock `generateTestSteps`, `queuedRequest`, `defaultApiQueue`, `calculateCost`
- Mock `nanoid` with `vi.mock()` to return deterministic IDs (e.g., 'test-id-1', 'test-id-2')
- `generateSteps(params)`:
  - Calls queuedRequest with defaultApiQueue and a function that calls generateTestSteps
  - Assigns unique nanoid IDs to each step (RawTestStep -> step with id)
  - Calls calculateCost with usage and model
  - Returns { steps, reasoning, usage, cost } with correct shape
  - Steps array has id field added to each step
  - Cost is calculated from the model parameter

**video/storage.test.ts**:
- Mock `fs/promises` with `vi.mock()` -- mock mkdir, readFile, writeFile
- `saveVideo(testRunId, viewport, sourcePath)`:
  - Creates directory `{VIDEOS_DIR}/{testRunId}` recursively
  - Reads source file and writes to `{VIDEOS_DIR}/{testRunId}/{viewport}.webm`
  - Returns relative path `{testRunId}/{viewport}.webm`
- `getVideoPath(relativePath)`:
  - Returns absolute path `{VIDEOS_DIR}/{relativePath}`
  - Joins VIDEOS_DIR with relative path correctly

**reports/html-generator.test.ts**:
- Read source first to understand the `generateHtmlReport` function signature
- Test that it produces valid HTML string containing key elements:
  - Report title, test run info, step results
  - Pass/fail indicators
  - Screenshot images (if provided)
- Feed it mock ReportData with known values, assert output contains expected strings
- Test edge cases: no steps, all passing, all failing, mixed results

IMPORTANT: Worker tests may need to mock `@validater/core` and `@validater/db` imports. Use `vi.mock()` at the top of test files. For factory DI pattern tests (persist-results), the mock DB is injected directly -- no module mocking needed for the DB itself.
  </action>
  <verify>
Run `pnpm --filter @validater/worker exec vitest run` -- all tests pass.
Check test count: should be 20+ individual test cases across worker test files.
  </verify>
  <done>5 worker test files exist covering memory-monitor, persist-results, generate-steps, video/storage, and html-generator. All tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @validater/core exec vitest run` -- all core tests pass (65+ test cases)
2. `pnpm --filter @validater/worker exec vitest run` -- all worker tests pass (20+ test cases)
3. `pnpm test` from root -- both packages run without failures
4. No test imports real Playwright browsers or connects to real databases
5. All mock patterns use vi.fn() with proper type casting
6. Previously missing modules (step-executor, healer, client, generate-steps, video/storage) all have dedicated test files
</verification>

<success_criteria>
- Core package has test files for: cost-tracker, rate-limiter, client, schemas (3), simplifier, extractor, assertions, viewport-presets, step-runner, step-executor, mapper, validator, healer (15 test files)
- Worker package has test files for: memory-monitor, persist-results, generate-steps, video/storage, html-generator (5 test files)
- All 20 test files pass with `pnpm test`
- Tests cover happy path, error cases, and edge cases for each module
</success_criteria>

<output>
After completion, create `.planning/phases/10-quality-and-coverage/10-02-SUMMARY.md`
</output>
