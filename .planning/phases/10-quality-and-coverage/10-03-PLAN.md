---
phase: 10-quality-and-coverage
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/web/src/components/__tests__/accessibility-panel.test.tsx
  - packages/web/src/hooks/__tests__/use-test-run-polling.test.ts
  - packages/web/src/lib/__tests__/utils.test.ts
  - packages/core/src/generation/__tests__/pipeline.test.ts
  - packages/worker/src/activities/__tests__/execute-persist-integration.test.ts
  - vitest.config.ts
autonomous: true

must_haves:
  truths:
    - "UI component tests render with mock data and verify display output -- web package achieves 50%+ line coverage on tested modules"
    - "Hook tests verify state management and lifecycle behavior"
    - "Generation pipeline integration test verifies full orchestration with all dependencies mocked"
    - "Execution-to-persistence integration test verifies executeSteps result shapes flow correctly through persistResults"
    - "Coverage thresholds are tuned to actual coverage levels and pnpm test:coverage exits 0 with no threshold violations"
    - "All tests across all 3 packages pass from root pnpm test -- total 90+ test cases"
  artifacts:
    - path: "packages/web/src/components/__tests__/accessibility-panel.test.tsx"
      provides: "Component render tests for accessibility panel"
      min_lines: 40
    - path: "packages/web/src/hooks/__tests__/use-test-run-polling.test.ts"
      provides: "Hook lifecycle tests for polling behavior"
      min_lines: 40
    - path: "packages/web/src/lib/__tests__/utils.test.ts"
      provides: "Tests for cn() utility and any other lib utilities"
      min_lines: 15
    - path: "packages/core/src/generation/__tests__/pipeline.test.ts"
      provides: "Integration test for generateAndValidateTestSteps pipeline"
      min_lines: 60
    - path: "packages/worker/src/activities/__tests__/execute-persist-integration.test.ts"
      provides: "Integration test verifying execution result shapes persist correctly"
      min_lines: 50
    - path: "vitest.config.ts"
      provides: "Updated root config with tuned coverage thresholds"
      contains: "thresholds"
  key_links:
    - from: "packages/core/src/generation/__tests__/pipeline.test.ts"
      to: "packages/core/src/generation/pipeline.ts"
      via: "vi.mock for all pipeline dependencies"
      pattern: "vi\\.mock"
    - from: "packages/web/src/components/__tests__/accessibility-panel.test.tsx"
      to: "packages/web/src/components/accessibility-panel.tsx"
      via: "render() from @testing-library/react"
      pattern: "render.*AccessibilityPanel"
    - from: "packages/worker/src/activities/__tests__/execute-persist-integration.test.ts"
      to: "packages/worker/src/activities/persist-results.activity.ts"
      via: "createPersistActivities with mock DB verifying data shapes from ExecutionResult"
      pattern: "createPersistActivities"
---

<objective>
Write UI component tests, hook tests, generation pipeline integration test, execution-to-persistence integration test, then tune coverage thresholds to match actual levels.

Purpose: Achieve the 50%+ UI coverage target, verify end-to-end pipeline integration for both generation and execution-persistence paths, then finalize coverage thresholds based on real numbers.
Output: Web package component/hook tests, core package generation integration test, worker package execute-persist integration test, and tuned vitest.config.ts coverage thresholds.
</objective>

<execution_context>
@/Users/karthiksivadas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karthiksivadas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-and-coverage/10-RESEARCH.md
@.planning/phases/10-quality-and-coverage/10-01-SUMMARY.md
@.planning/phases/10-quality-and-coverage/10-02-SUMMARY.md

Source files to test:
@packages/web/src/components/accessibility-panel.tsx
@packages/web/src/hooks/use-test-run-polling.ts
@packages/web/src/lib/utils.ts
@packages/core/src/generation/pipeline.ts
@packages/worker/src/activities/persist-results.activity.ts
@packages/worker/src/activities/execute-steps.activity.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web package UI tests, generation pipeline integration test, and execution-to-persistence integration test</name>
  <files>
    packages/web/src/components/__tests__/accessibility-panel.test.tsx
    packages/web/src/hooks/__tests__/use-test-run-polling.test.ts
    packages/web/src/lib/__tests__/utils.test.ts
    packages/core/src/generation/__tests__/pipeline.test.ts
    packages/worker/src/activities/__tests__/execute-persist-integration.test.ts
  </files>
  <action>
**web/components/__tests__/accessibility-panel.test.tsx**:
- Read accessibility-panel.tsx source first to understand props interface and rendering
- Import `{ render, screen }` from `@testing-library/react`
- Test rendering with mock accessibility data (violations array with severity levels)
- Verify: displays violation count, shows severity badges (critical, serious, moderate, minor), renders violation descriptions, handles empty violations array (no issues state), renders help URLs if present
- Do NOT test shadcn/ui primitives -- only test the custom component logic

**web/hooks/__tests__/use-test-run-polling.test.ts**:
- Read use-test-run-polling.ts source first to understand the hook interface
- Use `vi.useFakeTimers()` for timer control
- Mock the server function the hook calls (likely getTestRunStatusFn or similar)
- Test: starts polling on mount with given testRunId, stops polling when status is 'complete' or 'failed', returns current status data, clears interval on unmount
- Use `renderHook` from `@testing-library/react` (import from `@testing-library/react`)
- Use `act()` to advance timers: `vi.advanceTimersByTime(interval)`

**web/lib/__tests__/utils.test.ts**:
- Read utils.ts source first -- likely exports `cn()` (clsx + tailwind-merge)
- Test: merges class names, handles conditional classes, deduplicates Tailwind classes (e.g., `cn('p-4', 'p-2')` -> `'p-2'`)

**core/generation/__tests__/pipeline.test.ts** (generation integration test):
- This verifies the full GENERATION pipeline orchestration
- Read pipeline.ts source to understand imports and call flow
- Use `vi.mock()` at file top to mock ALL dependencies:
  - `../dom/crawler.js` -> crawlPage returns mock HTML + ariaSnapshot
  - `../dom/simplifier.js` -> simplifyDom returns mock SimplifiedDom
  - `../ai/client.js` -> generateTestSteps returns mock steps with usage
  - `../ai/rate-limiter.js` -> queuedRequest passes through fn()
  - `../locators/validator.js` -> verifyStepLocators returns all valid
  - `../locators/healer.js` -> healStepLocators returns steps unchanged
- Import `createMockPage` from `../__test-utils__/index.js`
- Test cases:
  - Full pipeline success: crawl -> simplify -> generate -> validate -> result with steps, cost, durationMs
  - Steps get nanoid IDs assigned (not undefined)
  - Cost tracking: result.cost has totalCost > 0
  - Pipeline returns durationMs > 0
  - Healer is NOT called when all locators are valid
  - When primary locator is invalid, healer IS called (mock verifyStepLocators to return primaryLocatorValid: false)

**worker/activities/__tests__/execute-persist-integration.test.ts** (execution-to-persistence integration test):
- This verifies that EXECUTION result shapes flow correctly into PERSISTENCE
- This does NOT test executeStepsActivity (which needs real browser) -- it tests that the data shapes produced by the execution layer are compatible with persistResults
- Import `createMockDb` from `@validater/core/__test-utils__`
- Import `createPersistActivities` from `../persist-results.activity.js`
- Import `ExecutionResult` type from `@validater/core`
- Build realistic ExecutionResult objects that match what executeStepsActivity produces:
  ```typescript
  const realisticResult: ExecutionResult = {
    viewport: 'desktop',
    url: 'https://example.com',
    stepResults: [
      {
        stepId: 'step-1',
        stepOrder: 1,
        action: 'navigate',
        description: 'Go to homepage',
        status: 'passed',
        durationMs: 1500,
        screenshotBase64: '',  // stripped for Temporal payload
        error: undefined,
      },
      {
        stepId: 'step-2',
        stepOrder: 2,
        action: 'click',
        description: 'Click login button',
        status: 'failed',
        durationMs: 500,
        screenshotBase64: '',
        error: { message: 'Element not found', expected: 'visible', actual: 'hidden' },
      },
    ],
    totalDurationMs: 2000,
    startedAt: '2026-01-01T00:00:00.000Z',
    completedAt: '2026-01-01T00:00:02.000Z',
    videoPath: 'test-run-1/desktop.webm',
    accessibilityData: {
      violationCount: 2,
      passCount: 10,
      incompleteCount: 1,
      inapplicableCount: 5,
      violations: [{
        id: 'color-contrast',
        impact: 'serious',
        description: 'Elements must have sufficient color contrast',
        help: 'Fix contrast',
        helpUrl: 'https://dequeuniversity.com/rules/axe/4.x/color-contrast',
        tags: ['wcag2aa'],
        nodes: [{ target: ['button.submit'], html: '<button class="submit">Submit</button>', impact: 'serious', failureSummary: 'Fix contrast ratio' }],
        nodeCount: 1,
      }],
    },
  };
  ```
- Test cases:
  - `persistResults({ testRunId: 'run-1', results: [realisticResult] })` completes without error
  - Verify db.insert was called for testRunResults with correct viewport, url, totalDurationMs, videoPath
  - Verify db.insert was called for testRunSteps for each stepResult with correct action, description, status, errorMessage
  - Verify db.insert was called for accessibilityResults with violation data
  - Verify db.delete was called on stepScreenshots to clean up staging table
  - Verify db.update was called on testRuns to set status='complete'
  - Multi-viewport: pass 2 ExecutionResult objects (desktop + mobile), verify both are persisted
  - stepResult with error: verify errorMessage, errorExpected, errorActual are mapped correctly
  - stepResult with screenshotBase64='': verify it falls back to staging table lookup (db.select called)
- Mock the staging table select to return a cached screenshot for one step, verify it's used in the insert

Use `import { vi, describe, it, expect, beforeEach } from 'vitest'` in all files.
For web tests, use `.ts`/`.tsx` extensions. For core tests, use `.js` extensions in source imports for NodeNext.
  </action>
  <verify>
Run `pnpm --filter @validater/web exec vitest run` -- web tests pass.
Run `pnpm --filter @validater/core exec vitest run` -- core tests pass (including new pipeline integration test).
Run `pnpm --filter @validater/worker exec vitest run` -- worker tests pass (including new execute-persist integration test).
Run `pnpm test` from root -- all packages pass.
  </verify>
  <done>Web component/hook/utility tests exist. Core generation integration test exists. Worker execution-to-persistence integration test exists verifying realistic data flows. All tests pass from root.</done>
</task>

<task type="auto">
  <name>Task 2: Tune coverage thresholds and verify final coverage report</name>
  <files>
    vitest.config.ts
  </files>
  <action>
1. Run `pnpm test:coverage` from root and capture the coverage output.

2. Analyze the coverage report to determine actual coverage percentages per module area:
   - Business logic (packages/core/src/ai, dom, execution, locators, schemas, generation)
   - Service layer (packages/worker/src/activities, browser, reports, video)
   - UI (packages/web/src/components, hooks, lib)

3. Update `vitest.config.ts` coverage thresholds:
   - Set global floor to a reasonable level based on actual overall coverage (probably 40-60%)
   - If Vitest glob pattern thresholds work for monorepo paths, add per-area thresholds:
     - Business logic areas -> set threshold at actual coverage minus 5% buffer (target: approach 90%)
     - Service layer areas -> set threshold at actual coverage minus 5% buffer (target: approach 75%)
     - UI areas -> set threshold at actual coverage minus 5% buffer (target: approach 50%)
   - If glob patterns do NOT resolve correctly (see RESEARCH.md open question 1), use file-level thresholds for the most critical modules only:
     ```
     'packages/core/src/ai/cost-tracker.ts': { lines: 90, functions: 90 }
     'packages/core/src/ai/rate-limiter.ts': { lines: 85, functions: 85 }
     ```
   - The goal is thresholds that PASS now and prevent regression, not aspirational numbers that fail CI

4. Run `pnpm test:coverage` again and verify it exits 0 with all thresholds met.

5. Ensure `pnpm test` (without coverage) also passes cleanly.

NOTE: Do NOT set thresholds higher than actual coverage. The purpose is regression prevention. If actual coverage for core/dom is 85%, set threshold at 80%. If actual coverage for web/hooks is 55%, set threshold at 50%. Aspirational targets are documented in REQUIREMENTS.md; thresholds are enforcement.
  </action>
  <verify>
Run `pnpm test:coverage` -- exits 0 with all thresholds met (no threshold violations).
Run `pnpm test` -- all tests pass without coverage overhead.
Review coverage text output to confirm business logic modules have meaningful coverage.
  </verify>
  <done>Coverage thresholds are tuned to actual levels with regression-prevention buffer. `pnpm test:coverage` passes cleanly. Coverage report shows meaningful numbers for all three tiers.</done>
</task>

</tasks>

<verification>
1. `pnpm test` from root -- all tests pass across core, worker, web (zero failures)
2. `pnpm test:coverage` from root -- produces unified coverage report AND passes all thresholds
3. Coverage report shows non-trivial coverage for:
   - packages/core: business logic modules have substantial coverage (target 90%+)
   - packages/worker: service modules have test coverage (target 75%+)
   - packages/web: components and hooks have test coverage (target 50%+)
4. No test requires running infrastructure (Postgres, Redis, Temporal, real browsers)
5. Threshold values in vitest.config.ts match or are slightly below actual coverage levels
6. Execution-to-persistence integration test uses realistic data shapes matching actual ExecutionResult
</verification>

<success_criteria>
- Web package has test files for: accessibility-panel, use-test-run-polling, utils (3 test files)
- Core package has pipeline integration test (1 test file)
- Worker package has execution-to-persistence integration test (1 test file)
- Coverage thresholds configured and passing
- `pnpm test:coverage` exits 0
- Total test count across all packages: 90+ individual test cases
</success_criteria>

<output>
After completion, create `.planning/phases/10-quality-and-coverage/10-03-SUMMARY.md`
</output>
