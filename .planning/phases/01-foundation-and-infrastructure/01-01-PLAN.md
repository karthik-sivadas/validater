---
phase: 01-foundation-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
  - biome.json
  - tsconfig.json
  - .nvmrc
  - .env.example
  - .gitignore
  - packages/web/package.json
  - packages/web/tsconfig.json
  - packages/web/components.json
  - packages/web/app.config.ts
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/worker/package.json
  - packages/worker/tsconfig.json
autonomous: true

must_haves:
  truths:
    - "All monorepo packages exist with correct workspace references"
    - "pnpm install succeeds with no errors"
    - "turbo build runs across all packages"
    - "TypeScript path aliases resolve correctly in packages/web"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package definition"
      contains: "packages/*"
    - path: "turbo.json"
      provides: "Turborepo pipeline configuration"
      contains: "dependsOn"
    - path: "packages/web/package.json"
      provides: "TanStack Start web app package"
      contains: "@validater/db"
    - path: "packages/db/package.json"
      provides: "Database package"
      contains: "@validater/core"
    - path: "packages/core/package.json"
      provides: "Shared types package"
      contains: "zod"
    - path: "packages/worker/package.json"
      provides: "Temporal worker package"
      contains: "@temporalio"
  key_links:
    - from: "packages/web/package.json"
      to: "packages/db"
      via: "workspace:* dependency"
      pattern: '"@validater/db":\\s*"workspace:\\*"'
    - from: "packages/web/package.json"
      to: "packages/core"
      via: "workspace:* dependency"
      pattern: '"@validater/core":\\s*"workspace:\\*"'
---

<objective>
Scaffold the Validater monorepo: create a TanStack Start app with shadcn/ui, then restructure into a pnpm workspaces + Turborepo monorepo with four packages (web, db, core, worker).

Purpose: Establish the foundational project structure that all subsequent plans build upon.
Output: A working monorepo where `pnpm install` and `turbo build` succeed.
</objective>

<execution_context>
@/Users/karthiksivadas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karthiksivadas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold TanStack Start app with shadcn and restructure into monorepo</name>
  <files>
    pnpm-workspace.yaml
    turbo.json
    package.json
    biome.json
    tsconfig.json
    .nvmrc
    .env.example
    .gitignore
    packages/web/ (all scaffolded files)
  </files>
  <action>
1. Scaffold the TanStack Start app with shadcn. Try the preset URL command first:
   ```
   bunx --bun shadcn@latest create --rtl --preset "https://ui.shadcn.com/init?base=base&style=lyra&baseColor=gray&theme=emerald&iconLibrary=remixicon&font=inter&menuAccent=subtle&menuColor=default&radius=default&template=start&rtl=true" --template start validater-web
   ```
   If that fails, fall back to: `pnpm create @tanstack/start@latest validater-web --tailwind --add-ons shadcn`
   Then configure shadcn manually with Lyra style, emerald theme, Inter font, Remix icons, 0px radius.

2. Move the scaffolded app into `packages/web/`:
   - Move all generated files from `validater-web/` into `packages/web/`
   - Delete the temporary `validater-web/` directory

3. Create root `pnpm-workspace.yaml`:
   ```yaml
   packages:
     - "packages/*"
   ```

4. Create root `package.json` with workspace scripts (dev, build, typecheck, lint, test, db:push, db:generate, db:migrate, docker:up, docker:down, temporal:worker). Use `pnpm@9.15.0` as packageManager. Set engines to node >=22.0.0. See research for exact script definitions.

5. Create root `turbo.json` with tasks: build (dependsOn: [^build], outputs: [.output/**, dist/**]), dev (cache: false, persistent: true), typecheck (dependsOn: [^build]), lint, test.

6. Create root `biome.json` with recommended rules. Set indent to tabs, line width 100, organize imports enabled.

7. Create root `tsconfig.json` with project references to all packages.

8. Create `.nvmrc` with `22` (Node.js 22).

9. Create `.env.example` with placeholder values:
   ```
   DATABASE_URL=postgres://validater:validater@localhost:5432/validater
   BETTER_AUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32
   BETTER_AUTH_URL=http://localhost:3000
   ```

10. Update `.gitignore` to include: node_modules, .output, dist, .env, .turbo, *.tsbuildinfo, .vinxi, .temporal.

11. Update `packages/web/tsconfig.json` paths so `@/*` resolves to `./src/*`. Verify `components.json` aliases match.

12. Update `packages/web/package.json`: set name to `@validater/web`, add workspace dependencies `"@validater/db": "workspace:*"` and `"@validater/core": "workspace:*"`.
  </action>
  <verify>
    Run `pnpm install` at repo root -- must succeed with no errors.
    Run `ls packages/web/src/routes` -- must show route files from scaffold.
    Run `ls packages/web/src/components/ui` -- must show shadcn components.
  </verify>
  <done>Scaffolded TanStack Start app lives in packages/web/ with correct shadcn config (Lyra, emerald, Inter, Remix icons). Root workspace files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create internal packages (db, core, worker) with workspace wiring</name>
  <files>
    packages/db/package.json
    packages/db/tsconfig.json
    packages/db/src/index.ts
    packages/core/package.json
    packages/core/tsconfig.json
    packages/core/src/index.ts
    packages/core/src/types/index.ts
    packages/core/src/schemas/index.ts
    packages/worker/package.json
    packages/worker/tsconfig.json
    packages/worker/src/index.ts
  </files>
  <action>
1. Create `packages/db/` package:
   - `package.json`: name `@validater/db`, private, type module, exports `.` -> `./src/client.ts` and `./schema` -> `./src/schema/index.ts`. Scripts: db:generate, db:migrate, db:push, db:studio. Dependencies: drizzle-orm, pg, dotenv. DevDeps: drizzle-kit, @types/pg, typescript. See research for exact structure.
   - `tsconfig.json`: extends root, compilerOptions with module NodeNext, moduleResolution NodeNext, target ES2022, strict true, outDir dist.
   - `src/index.ts`: placeholder export `// Database client - implemented in plan 01-02`

2. Create `packages/core/` package:
   - `package.json`: name `@validater/core`, private, type module, exports `.` -> `./src/index.ts`, `./types` -> `./src/types/index.ts`, `./schemas` -> `./src/schemas/index.ts`. Dependencies: zod. DevDeps: typescript.
   - `tsconfig.json`: extends root, same compiler options as db.
   - `src/index.ts`: barrel re-export from types and schemas.
   - `src/types/index.ts`: placeholder `export type AppConfig = { appName: string };`
   - `src/schemas/index.ts`: placeholder `import { z } from "zod"; export const appConfigSchema = z.object({ appName: z.string() });`

3. Create `packages/worker/` package:
   - `package.json`: name `@validater/worker`, private, type module, scripts: { start: "tsx src/worker.ts", build: "tsc" }. Dependencies: @temporalio/client, @temporalio/worker, @temporalio/workflow, @temporalio/activity, @validater/core (workspace:*), @validater/db (workspace:*). DevDeps: typescript, tsx.
   - `tsconfig.json`: extends root, same compiler options.
   - `src/index.ts`: placeholder `// Worker entry point - implemented in plan 01-04`

4. Run `pnpm install` at repo root to link all workspaces.

5. Install root devDependencies: `pnpm add -Dw turbo @biomejs/biome typescript`

6. Verify turbo can see all packages: `pnpm turbo build --dry-run`
  </action>
  <verify>
    Run `pnpm turbo build --dry-run` -- must list all 4 packages (web, db, core, worker).
    Run `pnpm ls -r --depth 0` -- must show all @validater/* packages.
    Run `pnpm turbo typecheck` -- must pass (placeholder files are valid TypeScript).
  </verify>
  <done>Four workspace packages exist with correct names, exports, workspace:* cross-references, and Turborepo can orchestrate builds across all of them.</done>
</task>

</tasks>

<verification>
1. `pnpm install` completes without errors
2. `pnpm turbo build --dry-run` lists all 4 packages
3. `pnpm turbo typecheck` passes
4. `packages/web/src/components/ui/` contains shadcn components
5. `cat packages/web/components.json` shows Lyra style config
6. All workspace:* references resolve correctly
</verification>

<success_criteria>
- Monorepo structure matches the architecture from research (packages/web, db, core, worker)
- pnpm install and turbo typecheck both pass
- shadcn/ui is configured with Lyra style, emerald theme, Inter font, Remix icons
- Internal packages reference each other via workspace:* protocol
- Node.js 22 pinned in .nvmrc
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md`
</output>
