---
phase: 06.1-step-details-screenshots-browser-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/execution/types.ts
  - packages/core/src/execution/step-runner.ts
  - packages/db/src/schema/test-runs.ts
  - packages/db/drizzle.config.ts
  - packages/worker/src/streaming/types.ts
  - packages/worker/src/activities/execute-steps.activity.ts
  - packages/worker/src/activities/persist-results.activity.ts
  - packages/worker/src/worker.ts
  - packages/worker/src/workflows/viewport-execution.workflow.ts
  - packages/worker/src/streaming/screencast.ts
autonomous: true

must_haves:
  truths:
    - "StepResult carries action and description from TestStep through the entire pipeline"
    - "Screenshots are persisted to database for ALL viewports, not lost during Temporal return"
    - "StepEvent published to Redis includes action and description for live streaming"
    - "Database test_run_steps rows contain action, description, and screenshot data"
    - "CDP screencast uses quality 80 and everyNthFrame 1 for smoother frames"
  artifacts:
    - path: "packages/core/src/execution/types.ts"
      provides: "StepResult with action and description fields"
      contains: "action: string"
    - path: "packages/db/src/schema/test-runs.ts"
      provides: "test_run_steps with action/description columns, step_screenshots staging table"
      contains: "stepScreenshots"
    - path: "packages/worker/src/activities/execute-steps.activity.ts"
      provides: "Factory function createExecuteActivities(db) that persists screenshots to staging table"
      exports: ["createExecuteActivities", "ExecuteActivities"]
    - path: "packages/worker/src/activities/persist-results.activity.ts"
      provides: "persistResults reads screenshots from staging table and writes to final rows"
      contains: "stepScreenshots"
    - path: "packages/worker/src/streaming/screencast.ts"
      provides: "Improved CDP params (quality 80, everyNthFrame 1)"
      contains: "quality: 80"
  key_links:
    - from: "packages/core/src/execution/step-runner.ts"
      to: "StepResult"
      via: "copies action/description from TestStep into return value"
      pattern: "action: step\\.action"
    - from: "packages/worker/src/activities/execute-steps.activity.ts"
      to: "packages/db/src/schema/test-runs.ts"
      via: "writes screenshots to stepScreenshots staging table"
      pattern: "db\\.insert\\(stepScreenshots\\)"
    - from: "packages/worker/src/activities/persist-results.activity.ts"
      to: "packages/db/src/schema/test-runs.ts"
      via: "reads from stepScreenshots when creating test_run_steps rows"
      pattern: "stepScreenshots"
    - from: "packages/worker/src/worker.ts"
      to: "packages/worker/src/activities/execute-steps.activity.ts"
      via: "uses createExecuteActivities(db) factory pattern"
      pattern: "createExecuteActivities"
---

<objective>
Enrich the step data pipeline end-to-end: add action/description to StepResult, persist screenshots via staging table (bypassing Temporal's 2MB payload limit), improve CDP streaming quality, and wire everything through the worker.

Purpose: This is the backend foundation for Phase 6.1. All frontend display improvements depend on this data being available in the database and streaming pipeline.

Output: Modified core types, DB schema with new columns and staging table, factory-pattern execute activity with screenshot persistence, updated persist-results that reads from staging, improved CDP screencast params.
</objective>

<execution_context>
@/Users/karthiksivadas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karthiksivadas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-step-details-screenshots-browser-experience/06.1-RESEARCH.md

Key source files to read before modifying:
@packages/core/src/execution/types.ts
@packages/core/src/execution/step-runner.ts
@packages/core/src/execution/step-executor.ts
@packages/core/src/types/test-step.ts
@packages/db/src/schema/test-runs.ts
@packages/db/drizzle.config.ts
@packages/worker/src/streaming/types.ts
@packages/worker/src/streaming/screencast.ts
@packages/worker/src/streaming/redis-publisher.ts
@packages/worker/src/activities/execute-steps.activity.ts
@packages/worker/src/activities/persist-results.activity.ts
@packages/worker/src/worker.ts
@packages/worker/src/workflows/viewport-execution.workflow.ts
@packages/worker/src/workflows/test-run.workflow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich types, schema, and step metadata propagation</name>
  <files>
    packages/core/src/execution/types.ts
    packages/core/src/execution/step-runner.ts
    packages/db/src/schema/test-runs.ts
    packages/worker/src/streaming/types.ts
    packages/worker/src/streaming/screencast.ts
  </files>
  <action>
    1. **StepResult type** (`packages/core/src/execution/types.ts`):
       Add `action: string` and `description: string` fields to the `StepResult` interface, after `stepOrder`. These carry the action type (click, fill, assert, etc.) and human-readable description from TestStep through the pipeline.

    2. **step-runner.ts** (`packages/core/src/execution/step-runner.ts`):
       In the `executeStep` function return statement (line ~158), add `action: step.action` and `description: step.description` to propagate from TestStep to StepResult. The TestStep is already in scope as the `step` parameter.

    3. **DB schema** (`packages/db/src/schema/test-runs.ts`):
       - Add `action: text("action")` column to `testRunSteps` table (after `stepOrder`, before `status`). Nullable -- existing rows won't have it.
       - Add `description: text("description")` column to `testRunSteps` table (after `action`). Nullable.
       - Add a new `stepScreenshots` staging table:
         ```
         export const stepScreenshots = pgTable("step_screenshots", {
           id: text("id").primaryKey(),
           testRunId: text("test_run_id").notNull(),
           viewport: text("viewport").notNull(),
           stepOrder: integer("step_order").notNull(),
           screenshotBase64: text("screenshot_base64").notNull(),
         });
         ```
       - Add a unique index on `(testRunId, viewport, stepOrder)` using drizzle's `unique()` or `.unique()` constraint so that retry idempotency uses `onConflictDoUpdate`. Import `unique` from `drizzle-orm/pg-core` if needed, or use the `pgTable` second argument for composite unique. The simplest approach:
         ```
         import { uniqueIndex } from "drizzle-orm/pg-core";
         // ... in stepScreenshots table definition, or after:
         // Use .unique() on a composite of testRunId + viewport + stepOrder
         ```
         Alternatively, add the unique constraint inline using drizzle's table config callback (second arg to pgTable).

    4. **drizzle.config.ts** (`packages/db/drizzle.config.ts`):
       The `stepScreenshots` table is in the SAME file (`test-runs.ts`), so no config change needed. But verify this -- if you add a new schema file, add it to the schema array.

    5. **Worker StepEvent type** (`packages/worker/src/streaming/types.ts`):
       Add `action: string` and `description: string` fields to the `StepEvent` interface (after `status`, before `durationMs`).

    6. **CDP screencast quality** (`packages/worker/src/streaming/screencast.ts`):
       Change `quality: 60` to `quality: 80` and `everyNthFrame: 2` to `everyNthFrame: 1` in the `Page.startScreencast` call.

    7. **Run schema push**: From `packages/db`, run `pnpm drizzle-kit push` to apply the new columns and staging table to the database. Ensure PostgreSQL is running first (`podman compose up -d` from monorepo root if needed).
  </action>
  <verify>
    - `cd packages/db && pnpm drizzle-kit push` completes without errors
    - `grep -n "action: string" packages/core/src/execution/types.ts` shows the new field in StepResult
    - `grep -n "action: step.action" packages/core/src/execution/step-runner.ts` shows propagation
    - `grep -n "stepScreenshots" packages/db/src/schema/test-runs.ts` shows the staging table
    - `grep -n "quality: 80" packages/worker/src/streaming/screencast.ts` shows improved CDP params
    - `grep -n "action: string" packages/worker/src/streaming/types.ts` shows enriched StepEvent
    - `cd packages/core && pnpm tsc --noEmit` passes
    - `cd packages/db && pnpm tsc --noEmit` passes
  </verify>
  <done>
    StepResult has action+description, DB has new columns and staging table, StepEvent has action+description, CDP params improved. All types compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Factory pattern for execute activity, screenshot persistence, and persist-results wiring</name>
  <files>
    packages/worker/src/activities/execute-steps.activity.ts
    packages/worker/src/activities/persist-results.activity.ts
    packages/worker/src/worker.ts
    packages/worker/src/workflows/viewport-execution.workflow.ts
    packages/worker/src/workflows/test-run.workflow.ts
  </files>
  <action>
    1. **Convert executeStepsActivity to factory pattern** (`packages/worker/src/activities/execute-steps.activity.ts`):
       - Import `Database` type from `@validater/db` and `stepScreenshots` schema from `@validater/db`.
       - Import `nanoid` from `nanoid`.
       - Import `eq` and `and` from `drizzle-orm` (for future queries if needed, but primarily for the insert).
       - Wrap the existing `executeStepsActivity` function inside a factory function: `export function createExecuteActivities(db: Database)`.
       - Inside the `onStepComplete` callback (currently at line ~74), AFTER the heartbeat call, add screenshot persistence:
         ```typescript
         // Persist screenshot to staging table (side-channel, bypasses Temporal payload)
         if (result.screenshotBase64) {
           try {
             await db.insert(stepScreenshots).values({
               id: nanoid(),
               testRunId: params.streamingConfig?.testRunId ?? '',
               viewport: params.viewport.name,
               stepOrder: result.stepOrder,
               screenshotBase64: result.screenshotBase64,
             }).onConflictDoUpdate({
               target: [stepScreenshots.testRunId, stepScreenshots.viewport, stepScreenshots.stepOrder],
               set: { screenshotBase64: result.screenshotBase64 },
             });
           } catch {
             // Screenshot persist failure must not break execution
           }
         }
         ```
         NOTE: If the unique constraint approach doesn't work with the `target` array syntax, use `onConflictDoNothing()` as a fallback -- the latest screenshot from a retry is equivalent.
       - Update the `publishStepEvent` call to include `action: result.action` and `description: result.description` in the event object.
       - The existing screenshot stripping logic (line ~109) stays as-is -- screenshots are already persisted to staging.
       - Return the activity from the factory: `return { executeStepsActivity };`
       - Export a type alias: `export type ExecuteActivities = ReturnType<typeof createExecuteActivities>;`
       - Remove the direct `export async function executeStepsActivity` -- it's now inside the factory.
       - Keep the `ExecuteStepsParams` interface export as-is (it's used by the workflow).

    2. **Update persist-results to read from staging** (`packages/worker/src/activities/persist-results.activity.ts`):
       - Import `stepScreenshots` from `@validater/db`.
       - Import `and` from `drizzle-orm` (in addition to existing `eq`).
       - In the `persistResults` function, when inserting `testRunSteps` rows, for each stepResult:
         - Query `stepScreenshots` for a matching row: `db.select().from(stepScreenshots).where(and(eq(stepScreenshots.testRunId, params.testRunId), eq(stepScreenshots.viewport, result.viewport), eq(stepScreenshots.stepOrder, stepResult.stepOrder))).limit(1)`
         - Use the cached screenshot (if found) when inserting the `testRunSteps` row: `screenshotBase64: cached[0]?.screenshotBase64 ?? stepResult.screenshotBase64 ?? null`
         - Also write `action: stepResult.action` and `description: stepResult.description` to the testRunSteps row.
       - After the result loop completes (after all viewports processed), clean up staging: `await db.delete(stepScreenshots).where(eq(stepScreenshots.testRunId, params.testRunId));`

    3. **Update worker.ts** (`packages/worker/src/worker.ts`):
       - Change the execute activities import from `import * as executeActivities from ...` to `import { createExecuteActivities } from ...`.
       - After the existing `const persistActivities = createPersistActivities(db);`, add: `const executeActivities = createExecuteActivities(db);`
       - The spread in the Worker.create activities stays the same: `...executeActivities`.

    4. **Update viewport-execution workflow** (`packages/worker/src/workflows/viewport-execution.workflow.ts`):
       - Change the type import from `import type * as activities from '../activities/execute-steps.activity.js'` to `import type { ExecuteActivities } from '../activities/execute-steps.activity.js'`.
       - Change `proxyActivities<typeof activities>` to `proxyActivities<ExecuteActivities>` (same pattern as PersistActivities in test-run.workflow.ts).

    5. **Verify test-run.workflow.ts** (`packages/worker/src/workflows/test-run.workflow.ts`):
       - This file does NOT import execute activities directly (it delegates to the child workflow). No changes needed. Just confirm this.

    IMPORTANT: Follow the exact same factory + type alias pattern established by `createPersistActivities` / `PersistActivities` in persist-results.activity.ts. This is a proven pattern in this codebase.

    IMPORTANT: The `ExecuteStepsParams` interface must remain exported (not inside the factory) because it's referenced by the workflow types.
  </action>
  <verify>
    - `cd packages/worker && pnpm tsc --noEmit` passes (this validates the entire worker package including workflow types)
    - `grep -n "createExecuteActivities" packages/worker/src/worker.ts` shows factory usage
    - `grep -n "ExecuteActivities" packages/worker/src/workflows/viewport-execution.workflow.ts` shows type alias usage
    - `grep -n "stepScreenshots" packages/worker/src/activities/execute-steps.activity.ts` shows staging table write
    - `grep -n "stepScreenshots" packages/worker/src/activities/persist-results.activity.ts` shows staging table read and cleanup
    - `grep -n "action: result.action" packages/worker/src/activities/execute-steps.activity.ts` shows enriched step event
  </verify>
  <done>
    executeStepsActivity uses factory pattern with DB injection, screenshots are persisted to staging table during execution, persistResults reads screenshots from staging and writes to final rows with action/description, worker.ts creates the factory. Full worker package type-checks cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd packages/core && pnpm tsc --noEmit` -- core types compile
2. `cd packages/db && pnpm tsc --noEmit` -- db schema compiles
3. `cd packages/worker && pnpm tsc --noEmit` -- worker compiles (includes workflow type validation)
4. `grep -rn "action.*description" packages/core/src/execution/types.ts` -- StepResult has both fields
5. `grep -rn "stepScreenshots" packages/db/src/schema/test-runs.ts` -- staging table exists
6. `grep -rn "createExecuteActivities" packages/worker/src/worker.ts` -- factory pattern used
7. `grep -rn "quality: 80" packages/worker/src/streaming/screencast.ts` -- CDP quality improved
</verification>

<success_criteria>
- StepResult type includes action and description fields
- step-runner.ts propagates action/description from TestStep to StepResult
- test_run_steps has action and description columns in the database
- step_screenshots staging table exists with unique constraint on (testRunId, viewport, stepOrder)
- executeStepsActivity is a factory function (createExecuteActivities) with DB injection
- Screenshots are written to staging table during step execution
- persistResults reads from staging table and writes screenshots + action/description to final rows
- Staging table is cleaned up after persist completes
- StepEvent type (worker) includes action and description
- CDP screencast uses quality 80 and everyNthFrame 1
- All three packages (core, db, worker) type-check cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-step-details-screenshots-browser-experience/06.1-01-SUMMARY.md`
</output>
